# Demonstration data

Run `make demo` to generate `data/generated/lunar_south_pole_demo.json`.

The generated scene is a deterministic lunar south-polar analogue for software validation. It is not a Chandrayaan-2 observation and must not be represented as measured ice abundance, landing-site certification or an operational rover route.
